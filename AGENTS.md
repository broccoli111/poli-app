## Cursor Cloud specific instructions

This is a greenfield repository (`poli-app`). As of initial setup, it contains only a single empty placeholder file (`mvp`) and no application code, dependencies, or configuration.

### Current state

- No application framework, language runtime, or package manager has been chosen yet.
- No `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or similar files exist.
- There are no lint, test, or build commands to run.
- There are no services to start.

### When application code is added

Once the project is scaffolded with actual code, this section should be updated with:

- How to install dependencies (update script).
- How to run lint, tests, and the dev server.
- Any non-obvious caveats discovered during setup.
